Read `AGENTS.md` first. Claude must use the canonical control-path and health policies when choosing workstation routes: background Local Compute and isolated browser automation precede native desktop; S3 HID and Comet KVM are real-console fallbacks; OOB power/media is recovery-only. Keep MCP registration/session truth separate from repository source presence, preserve zero-cost constraints, and never broaden structured routes into arbitrary shell authority.

{
  "name": "@evavo/remote-mcp-relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare-hosted remote MCP and outbound workstation relay for EVAVO local execution readiness.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "type-check": "wrangler types && tsc --noEmit",
    "build": "wrangler deploy --dry-run",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "agents": "^0.20.0",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "wrangler": "^4.30.0"
  }
}
